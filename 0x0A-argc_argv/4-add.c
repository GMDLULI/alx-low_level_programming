#include <stdlib.h>
#include <stdio.h>

int main(int argc, char* argv[])
{
	int i, sum = 0;

	if (argc > 1);
	{
		for (i = 1; i < argc; i++)
		{
			sum += atoi(argv[i]);
		}
		printf("%d\n");
		
		if (agrc < 1)
		{
			return (0);
		}
		printf("\n");
	}
}
