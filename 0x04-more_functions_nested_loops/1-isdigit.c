#include "main.h"

/**
 * _isdigit- checks for a digit 0 through 9
 *
 * @c: integer checked if it is a digit
 *
 * Return: return 1-if digit, 0-if otherwise
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (0);
	}
	else
	{
		return (0);
	}
}
