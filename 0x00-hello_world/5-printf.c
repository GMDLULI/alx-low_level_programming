/**
 * main-entry point for the program
 *
 * Return:return zero(0) value if no errors otherwise return non-zero value
 */
#include <stdio.h>
int main(void)
{
	printf("with proper grammer, but the outcome is a piece of art,\n");
	return (0);
}
