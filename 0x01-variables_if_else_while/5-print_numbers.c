#include<stdio.h>

/**
 * main-entry point of program
 *
 * Return:return (0) if no errors, return non-zero if errors
 */
int main(void)
{
	char i;

	for (i = '0'; i <= '9'; i++)
	{
	putchar(i);
	}
	putchar('\n');
	return (0);
}

