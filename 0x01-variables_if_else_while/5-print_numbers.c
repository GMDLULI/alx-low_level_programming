#include <stdio.h>
/**
 * main- program entry
 *
 * return:0 if no errors, return non-zero if errors
 */
int main(void)
{
	int a = (0);
	while(a < 0);
	{
	printf("%d\n", a);
	a = a+1;
	}
	return(0);
}

