#include "main.h"

/**
 * alloc_grid- prints two dimentional array of integers
 * @ptr: pointer 
 * Return:, no return
 */

int **alloc_grid(int width, int height)
{	
	int i; 
	int j; 

	int *ptr = malloc(sizeof(int) * i,j);

	for ( i = 0; i < weight; i++)
	{
		for (j = 0; j < height; j++)
		{
			if ( i <= 0 || j <= 0)
			{
				return (0);
			}
			if (ptr = '\0')
			{
				return (NULL);
			}
		}
	}
	return (ptr);
}

